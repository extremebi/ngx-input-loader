import { NgxInputLoaderDirective } from './ngx-input-loader.directive';

describe('NgxInputLoaderDirective', () => {
    it('should create an instance', () => {
        const directive = new NgxInputLoaderDirective();
        expect(directive).toBeTruthy();
    });
});
